# Simple Java Add Numbers App

This repository contains a simple Java program that adds two numbers provided as command-line arguments.

## Compile

```bash
javac src/AddNumbers.java
```

## Run

```bash
java -cp src AddNumbers <num1> <num2>
```

Replace `<num1>` and `<num2>` with the numbers you wish to add.
